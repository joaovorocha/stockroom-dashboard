# Legacy files

This folder contains older/duplicate files that were previously in the repo root.

The app serves pages and assets from `public/`, and server-side utilities live in `utils/`, `routes/`, and `middleware/`.

Nothing in `legacy/` is required for the app to run.

